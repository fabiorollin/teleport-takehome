apiVersion: v1
kind: Namespace
metadata:
  name: nginx-app
  labels:
    app.kubernetes.io/managed-by: teleport-takehome
    # Labels referenced by NetworkPolicy `to.namespaceSelector` rules.
    kubernetes.io/metadata.name: nginx-app
